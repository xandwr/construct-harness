ANTHROPIC_API_KEY=sk_...
MODEL=claude-opus-4-8
MEMORY_DB=db.sqlite
KB_DIR=kb
PORT=8787
COMPACT_AT=120000
CORS_ORIGIN=*
# Provider-hosted tools the Construct may run server-side. Unset = web_search +
# web_fetch (live web access). Comma-separated to choose (web_search, web_fetch,
# code_execution); "none" to disable.
SERVER_TOOLS=web_search,web_fetch

OPENAI_API_KEY=
